<Page x:Class="TourAgency2018.Views.Pages.ServicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Дополнительные услуги">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель фильтров -->
        <StackPanel Orientation="Horizontal" Margin="10,8,10,8" VerticalAlignment="Center">
            <TextBox x:Name="SearchTextBox"
                     Width="250"
                     Padding="5"
                     FontSize="14"
                     Text="Поиск..."
                     Foreground="Gray"
                     GotFocus="SearchTextBox_GotFocus"
                     LostFocus="SearchTextBox_LostFocus"
                     TextChanged="SearchTextBox_TextChanged"/>
            <Button Content="Сбросить"
                    Margin="8,0,0,0"
                    Padding="12,0"
                    Click="ClearSearch_Click"/>
            <TextBlock x:Name="RecordCountText"
                       Margin="15,0,0,0"
                       VerticalAlignment="Center"
                       FontSize="13"
                       Foreground="Gray"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="ServicesDataGrid"
                  Grid.Row="1"
                  Margin="10"
                  ItemsSource="{Binding ServicesList}"
                  SelectedItem="{Binding SelectedService, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeight="36"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Название услуги" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Используется в турах" Binding="{Binding ToursCount}" Width="180"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
