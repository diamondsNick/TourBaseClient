<Page x:Class="TourAgency2018.Views.Pages.HotelDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="500" d:DesignWidth="900"
      Title="HotelDetailPage"
      KeepAlive="False">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">

            <Button Content="← Назад"
                    HorizontalAlignment="Left"
                    Padding="15,5"
                    Margin="0,0,0,20"
                    Click="BackButton_Click"/>

            <Image x:Name="HotelImage"
                   HorizontalAlignment="Left"
                   MaxHeight="250" MaxWidth="400"
                   Stretch="Uniform"
                   Margin="0,0,0,15"/>

            <TextBlock x:Name="HotelName" FontSize="24" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,10"/>
            <TextBlock x:Name="HotelId" FontSize="13" Foreground="Gray" Margin="0,0,0,5"/>
            <TextBlock x:Name="HotelCountry" FontSize="15" Margin="0,0,0,5"/>
            <TextBlock x:Name="HotelStars" FontSize="15" Margin="0,0,0,5"/>
            <TextBlock x:Name="HotelMealType" FontSize="15" Margin="0,0,0,20"/>

            <TextBlock Text="Комментарии" FontSize="18" FontWeight="Bold" Margin="0,0,0,5"/>
            <ItemsControl x:Name="CommentsList" Margin="0,0,0,5">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" Margin="0,0,0,8" Padding="10">
                            <StackPanel>
                                <TextBlock Text="{Binding Author}" FontWeight="Bold" FontSize="13"/>
                                <TextBlock Text="{Binding Date}" FontSize="12" Foreground="Gray" Margin="0,2,0,4"/>
                                <TextBlock Text="{Binding Text}" FontSize="14" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock x:Name="NoCommentsText" FontSize="15" Text="Комментариев нет." Visibility="Collapsed"/>

        </StackPanel>
    </ScrollViewer>
</Page>
