<Page x:Class="TourAgency2018.Views.Pages.ClientsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="900"
      Title="Клиенты">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель поиска -->
        <StackPanel Orientation="Horizontal" Margin="10,8,10,8" VerticalAlignment="Center">
            <TextBox x:Name="SearchTextBox"
                     Width="280"
                     Padding="5"
                     FontSize="14"
                     Text="Поиск по ФИО..."
                     Foreground="Gray"
                     GotFocus="SearchTextBox_GotFocus"
                     LostFocus="SearchTextBox_LostFocus"
                     TextChanged="SearchTextBox_TextChanged"/>
            <Button Content="Сбросить"
                    Margin="8,0,0,0"
                    Padding="12,0"
                    Click="ClearSearch_Click"/>
            <TextBlock x:Name="RecordCountText"
                       Margin="15,0,0,0"
                       VerticalAlignment="Center"
                       FontSize="13"
                       Foreground="Gray"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="ClientsDataGrid"
                  Grid.Row="1"
                  Margin="10"
                  ItemsSource="{Binding ClientsList}"
                  SelectedItem="{Binding SelectedClient, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeight="36"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="55"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding Surname}" Width="160"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="130"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding Patronymic}" Width="160"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding DateOfBirth}" Width="130"/>
                <DataGridTextColumn Header="Серия" Binding="{Binding PassportSeries}" Width="80"/>
                <DataGridTextColumn Header="Номер" Binding="{Binding PassportNumber}" Width="100"/>
                <DataGridTextColumn Header="Заявок" Binding="{Binding ApplicationsCount}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
