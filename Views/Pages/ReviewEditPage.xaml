<Page x:Class="TourAgency2018.Views.Pages.ReviewEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="ReviewEditPage"
      KeepAlive="False">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">

            <!-- Информация об отеле -->
            <Image x:Name="HotelImage"
                   HorizontalAlignment="Left"
                   MaxHeight="250" MaxWidth="400"
                   Stretch="Uniform"
                   Margin="0,0,0,15"/>

            <TextBlock x:Name="HotelName" FontSize="24" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,8"/>
            <TextBlock x:Name="HotelCountry" FontSize="15" Margin="0,0,0,5"/>
            <TextBlock x:Name="HotelStars" FontSize="15" Margin="0,0,0,5"/>
            <TextBlock x:Name="HotelMealType" FontSize="15" Margin="0,0,0,20"/>

            <Separator Margin="0,0,0,20"/>

            <!-- Отзыв -->
            <TextBlock x:Name="PageTitle" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <TextBox x:Name="CommentBox"
                     FontSize="14"
                     Padding="8"
                     MinHeight="100"
                     Width="450"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal">
                <Button Content="Сохранить" Padding="20,8" Margin="0,0,10,0" Click="Save_Click"/>
                <Button Content="Отмена" Padding="20,8" Click="Cancel_Click"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
